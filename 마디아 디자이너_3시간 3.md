#layout grid
- 웹 디자인할 때 복잡한 요소를 잘 정리되어 보이게끔 레이아웃 구성하는 기능
- 우측 패널 -> layout guide 추가(+) -> layout guide settings에서 속성 선택 (colums(화면을 몇 개로 나누어서 볼 건지=12, 대부분 colums=12,14,16으로 설정), color=default(red), margin(좌우측 여백=60), gutter(박스 사이의 여백=32), type(정렬 측=보통은 stretch 설정)
- 웹 디자인뿐 아니라 아이콘 가이드 그릴 때도 사용 가능
